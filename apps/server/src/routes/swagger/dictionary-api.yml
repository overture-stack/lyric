# Description of Dictionary API

/dictionary/register:
  post:
    summary: Register new dictionary
    tags:
      - dictionary
    parameters:
      - name: categoryName
        in: formData
        type: string
        required: true
      - name: dictionaryName
        in: formData
        type: string
        required: true
      - name: version
        in: formData
        type: string
        required: true
    responses:
      200:
        description: Dictionary info
