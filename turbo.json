{
	"$schema": "https://turbo.build/schema.json",
	"dangerouslyDisablePackageManagerCheck": true,
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"]
		}
	},
	"remoteCache": {
		"enabled": false
	}
}
