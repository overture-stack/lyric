Replaced `plur` with `inflection` in `pluralizeSchemaName`, the root cause of a Maestro/Arranger indexing bug traced back to Lyric's own compound-view pluralization.

- `packages/data-provider/src/utils/submissionUtils.ts`: swapped `import plur from 'plur'` for `import { pluralize } from 'inflection'`; `plur` mispluralized `primary_diagnosis` to `primary_diagnosises` (a Latin `-is` ending it doesn't special-case for compound/snake_case identifiers), verified directly against the pinned version and the latest release, neither fixes it. `inflection` got every real schema name in this dictionary right out of the box, including the two that tripped up the alternatives considered (`plur`'s `primary_diagnosis`, and separately `pluralize`'s `specimen`, which it treats as an uncountable noun).
- `packages/data-provider/package.json`: dropped `plur`, added `inflection` (ships its own types, no `@types/` package needed).
- New test: `test/unit/utils/submissionUtils.spec.ts`, covering `pluralizeSchemaName` against a regular noun, a `y -> ies` noun, the Latin `-is` case, and the uncountable-noun edge case, none of which had any prior coverage.
- Full monorepo build and the full unit suite (277 tests, all passing) run clean.
- `.dev/roadmap.md`: logged a follow-on item to extend `PLURALIZE_SCHEMAS_ENABLED` from a boolean into a language selector (`none`/`english`/`french`), since the current flag is English-only by its own documented design.

Not yet pushed. Branch `fix/pluralize-schema-name-library`, based off `origin/main`, built in an isolated worktree since the actual local clone is still mid-merge on `atomic-registration`.
