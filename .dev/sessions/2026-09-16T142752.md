Completed the file-row-number feature: populated `submission_records.line_number` at upload time (the migration and model column were already added), then swapped the summary/download endpoints from the meaningless DB `recordId` to the file's actual 1-based row number, since a record's database id told the user nothing about where to find the problem in their spreadsheet.

- `packages/data-provider/src/utils/fileUtils.ts`: `readTextFile` now returns `{record, lineNumber}` pairs for every parsed row (previously the line number was computed only for parse-time errors and thrown away for successful rows)
- `packages/data-provider/src/utils/submissionUtils.ts`: `submissionInsertDataFromFiles`/`FileInsertResult` thread the line number through unchanged; `submissionProcessor.ts`'s `addFilesToSubmissionAsync` now persists it on insert
- `packages/data-provider/src/utils/submissionTypes.ts`, `submissionUtils.ts`: `SubmissionRecordFieldError.recordId` and `SubmissionErrorFieldReasonCount.recordIds` renamed to `rowNumber`/`rowNumbers`; `rowNumber` is optional (a record not sourced from a file upload, e.g. a JSON edit, has none) and is never recomputed after the fact, so deleting or adding other records never renumbers it
- `packages/data-provider/src/repository/submissionRecordsRepository.ts`: `getById`/`getByFileIds`/`getErrorsByFileId` select `lineNumber`
- `packages/data-provider/src/services/submission/submissionService.ts`: both error-reading methods pass the record's `lineNumber` through instead of its `id`
- CSV/TSV export (`formatFieldErrorsAsDelimitedText`) gained a leading `rowNumber` column
- `apps/server/swagger/schemas.yml`: `SubmissionErrorsSummaryResult.errorsByFieldAndReason[].rowNumbers` documented in place of `recordIds`
- Tests: unit coverage for the optional/undefined `rowNumber` path and its effect on grouping; integration coverage confirms a real file upload persists the correct 1-based line numbers end-to-end (header row + 1-based, matching the existing parse-error convention), and that the summary/download endpoints report them correctly, including a record with no line number
- Found this session's shell had no working Docker daemon, blocking integration-test verification until started; also reconfirmed the Node v14/`.nvmrc` gap from the prior session (logged there) by having to `nvm use` again this session
- Left in place, still unaddressed: the uncommitted `status === 'ok' || status === 'invalid'` change in `submissionProcessor.ts` flagged last session as unattributed and not something this session wrote; my `lineNumber` edit landed in the same function, immediately adjacent to it, but did not touch or resolve that line
