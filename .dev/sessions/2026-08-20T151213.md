Synced agentics to current.

- `AGENTS.md`: version tag bumped to the current agentics sync point; the dispatch table itself was already current against the canonical template, so no content changed beyond the tag.
- `.dev/tech-debt.md`: trimmed process-narrative phrasing ("found while...", "flagged in PR #208 review") from three entries, per `entry-formats.md`'s newly generalized rule that a durable entry states the defect and fix, not how it came to light.
