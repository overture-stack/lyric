Fixed Maestro indexing documents into the wrong Elasticsearch index when multiple repos share one Kafka topic. Kafka messages now carry `categoryId` (always) and `categoryAlias` (when set); added category alias as a new Lyric feature. Maestro-side changes tracked in that repo's own session history.

- `packages/data-model/src/models/dictionary_categories.ts`, migration `0014_youthful_magma.sql`: new nullable, unique `alias` column
- `utils/types.ts`: `ResultOnCommit`, `CategorySummary`, `CategoryDetailsResponse` gain `alias?: string`; `ListAllCategoriesResponse` consolidated into `CategorySummary`, kept as an exported alias for backward compatibility
- `external/kafkaPublisher.ts`, `services/submission/submissionProcessor.ts`: `categoryId`/`categoryAlias` added to every published message
- `repository/activeSubmissionRepository.ts`, `categoryRepository.ts`: alias included in category column selections; new `getCategoryByAlias`, `getCategoryByIdOrAlias` (alias wins on collision), `withAliasNormalized` helper; `save()`/`update()` now throw 409, not a generic 503, on a unique-constraint race
- `services/categoryService.ts`: `getDetails`/`listAll` resolve and normalize alias; new `assignAlias`/`unassignAlias` (`PUT`/`DELETE /category/:categoryId/alias`), both idempotent and audit-logged, deduplicated via a `toCategorySummary` helper
- `services/dictionaryService.ts`: `register()` accepts optional `alias`, creation-time only (400 on an existing category, 409 on a taken alias)
- `controllers/dictionaryController.ts`: **real bug found and fixed** — `alias` was never read from the request body or passed to `register()`, so register-with-alias never worked end to end despite the service logic being correct; wired through, and now returned in the response too
- `controllers/categoryController.ts`, `migrationController.ts`, `auditController.ts`, `validationController.ts`, `submittedDataController.ts`: resolve categoryId-or-alias via new shared `utils/resolveCategoryId.ts`, replacing direct `Number(req.params.categoryId)`
- `routers/categoryRouter.ts`: new `PUT`/`DELETE /category/:categoryId/alias` routes
- `utils/formatUtils.ts`, `schemas.ts`: new `isValidCategoryAlias`; `categoryAliasSchema`/`categoryAliasAssignRequestSchema` via `zod.infer`; `categoryIdSchema` accepts alias-shaped strings
- `config/config.ts`: `ValidatorEntry.categoryId` widened to `number | string`
- Swagger (`categories-api.yml`, `dictionary-api.yml`, `schemas.yml`, `parameters.yml`), `README.md`, `docs/02-lyric.md`, `docs/kafka-publishing.md`: alias documented throughout; extracted a shared `CategorySummary` swagger schema
- Tests: new/extended coverage for alias creation, resolution, assign/unassign, and shared categoryId-or-alias acceptance across all five routers
- `.dev/tech-debt.md`, `.dev/roadmap.md`: logged sequential-id enumeration, the category-admin privilege gap, `auditController`'s dead-code check, and the `sqon-builder` → `sqon` migration (blocked on the latter leaving RC); split "reassign" into rename/transfer in the roadmap
- `AGENTS.md`/`CLAUDE.md`: agentics upgraded 0.1.0 → 0.6.1; `.dev/sessions.md` migrated to `.dev/sessions/`
- Ran the full integration suite against real Postgres/MongoDB/Lectern for the first time this session: found and fixed the register-alias bug above, plus 4 test-only bugs (a wrong expected message, a `before`/`afterEach` hook mismatch, two routes needing a valid body/query the shared test helper didn't supply) and one flaky/environmental failure not chased further. 98 passing, 0 failing after fixes.
