First real agentics upgrade since initial adoption, from 0.9.0 to 0.14.0+unreleased (synced SHA `718a327ec813bd520eaa8cd4c5e39ae80b10d5f6`).

- `AGENTS.md`: rebuilt against the current canonical template in full (interaction parameters, critical constraints, the complete dispatch table including entries that didn't exist at 0.9.0: `upstream-check.md`, `context-economy.md`, `entry-formats.md`, `agent-troubleshooting.md`, `agent-index.md`, `docs/agent-security.md`, `persistence-map.md`, `definition-of-done.md`; the `AGENTS.roles/`/`AGENTS.softeng.md`/`AGENTS.overture.md` naming; the new memory-location, memory-hygiene, and initialization content), keeping this project's own customizations (the `README.md`/`DEVELOPMENT.md` pointer, the Project notes section). Corrected one stale project note in the process: "new tests use `node:test`" was never actually true, all 54 existing specs use mocha/chai/sinon and none use `node:test`, so the note now states the real, current convention instead.
- `.dev/tech-debt.md`: reformatted every entry to carry a separate `fix:` field per the new `entry-formats.md` schema rather than folding it into `context:`; removed the "existing tests use mocha/chai/sinon, not node:test" entry entirely, since it was tech debt against a convention that was never real, folding its one true fact (mocha/chai is correct, not a migration target) into the co-location entry it was artificially chained to instead.
- Confirmed no in-repo `.claude/memory/` directory exists (the specific anti-pattern the memory-location fix targets).
- Project memory created for the agentics-setup facts this project never recorded at initial adoption: Overture project and existing-setup answered from context; `roadmap_split` still open, pending the developer's direct answer.
- Registered in the global agent index under the confirmed peer-diffed id `agentics-55` (shared registration with Maestro, "Lyric and Maestro"); wrote the identity guard into this workspace's own shared memory (the agentics project's memory directory, since that's what this multi-root workspace's sessions actually load).

Not yet committed or pushed.
